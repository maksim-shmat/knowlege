// Variables
let age = 25;
const name = "John";


// Data Types
let isStudent = true;
let score = null;
let hight;  // Undefined


// Operators
let x = 10;
let y = 5;

let sum = x + y;  // Addiction
let difference = x - y;  // Subtraction
let product = x * y;  // Multiplication
let quotient = x / y;  // Divusion
let remainder = x % y;  // Modulus


// Comparison
let isEqual = x === y;  // Strict equality check
let isGreaterThan = x > y;  // Greater than check


// Logical
let logicalAnd = (x > 0) && (y > 0);  // Logical AND
let logicalOr = (x > 0) || (y > 0);  // Logical OR
let logicalNot = !(x === y);  // Logical NOT

// Arithmetic Operators:
+, -, *, /, %, ++ Increment, -- Decrement;

// Comparison Operators
== Equal to, != Not equal to, === Strict equal to, !== Strict not equal to;
<, >, >=, <=;

// Assignment Operators
= Assign, += Add and assign, -= Subtract and assign, *= Multiply and assign, /= Divide and assign, %= Modulus and assign


// Function Declaration
function greet(name) {
	console.log("Hello, " + name + "!");
}

let greet = function(name) {
	console.log("Hello, " + name + "!");
};

let greet = (name) => {
	console.log("Hello, " + name + "!");
};

function add(x, y) {
	return x + y;
}

let result = add(3, 5);
console.log(result);


